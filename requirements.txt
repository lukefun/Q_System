# Q_System 量化交易框架依赖
# Python 3.8.x (miniQMT 要求)

# 数据处理
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# 可视化 (可选)
matplotlib>=3.7.0,<4.0.0

# 配置管理 (可选)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# 网络请求 (xtquant 依赖)
requests>=2.30.0,<3.0.0
tqdm>=4.60.0,<5.0.0

# 注意: xtquant 不在此列出
# xtquant 通过以下方式安装:
#   1. QMT 客户端自动配置
#   2. pip install xtquant
#   3. 从 QMT 目录复制
